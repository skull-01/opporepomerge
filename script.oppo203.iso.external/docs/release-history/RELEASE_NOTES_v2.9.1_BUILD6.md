# Release Notes — v2.9.1 Build 6

Build 6 adds local verification and packaging helper scripts. It does not change playback behavior, OPPO command semantics, XML routing, NAS playback, startup auto-power, or hardware-control behavior.

Use `scripts/verify.sh` for the standard verification sequence and `scripts/package_release.sh` for local runtime ZIP/checksum/dev-source packaging.
