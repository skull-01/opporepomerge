# Coverage Report — v2.5.0 Build 5

Expected optimized coverage command:

```bash
PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 coverage run -m pytest -p no:ddtrace
coverage report
```

Final result:

```text
TOTAL 99%
```
