# Coverage Report — v2.9.10 Build 11

Coverage gate target: TOTAL 99%.

Source sharded coverage result:

```text
TOTAL 4439 statements, 44 missed, 1474 branches, 29 partial branches, 99% coverage
```

The local one-shot aggregate coverage command is known to risk wall-time timeout in this environment. Build 11 uses deterministic coverage shards with `coverage run -p`, `coverage combine`, and `coverage report -m`.
