# Hardware Validation — v2.9.14 Final

Hardware validation was not performed and is not claimed.

This is a software-verified release. Passing automated tests and release audits does not prove real OPPO, Chinoppo, Magnetar, Reavon, Kodi, NAS, TV, ADB, Roku, LG, Samsung, Sony, Panasonic, Vizio, or AVR hardware behavior. In particular the v2.9.14 additions — the six routing-by-monitor playback presets, the SVM3 (`#SVM 3`) verbose-mode monitor's `@UPL`/`@UTC` confirmation, the `http_handoff` launch path, the richer `oppo203iso-status.json` session report, and the `tcp_qpl_poll` default / bounded hold-mode robustness changes — are software-verified only. Real tester results must be recorded separately before any hardware path is described as validated.
