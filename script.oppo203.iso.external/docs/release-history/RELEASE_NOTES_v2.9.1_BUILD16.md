# Release Notes — v2.9.1 Build 16

Build 16 is a development-tooling cleanup release for localization extraction. It keeps `tools/make_pot.py` available as a legacy compatibility alias and directs new automation to `tools/i18n_extract.py`.

There are no runtime playback, OPPO command, XML routing, NAS adapter, startup auto-power, packaging semantics, or hardware-control changes.

Hardware validation was not performed and is not claimed.
