# Release Notes — v2.5.3 Build 3

Build 3 is a stability and packaging-identity build. It updates the runtime add-on version in `addon.xml` to `2.5.3` so the package identity and Kodi metadata match the v2.5.3 build line.

No runtime playback behavior changed. Build 1 service interception and Build 2 conservative XML routing remain the active v2.5.3 functionality.

Hardware validation is not claimed for this build.
