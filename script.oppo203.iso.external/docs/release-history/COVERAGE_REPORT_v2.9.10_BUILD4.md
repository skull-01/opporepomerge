# COVERAGE_REPORT_v2.9.10_BUILD4.md

```text
Source coverage: TOTAL 99%
Post-unpack dev-source coverage: TOTAL 99%
```

Build 4 preserves the 99% coverage gate. Coverage was measured with split coverage runs using `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` and `-p no:ddtrace` to avoid local plugin timeout behavior.
