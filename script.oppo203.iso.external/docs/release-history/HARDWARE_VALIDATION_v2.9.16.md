# Hardware Validation — v2.9.16 Final

Hardware validation was not performed and is not claimed.

This is a software-verified release. Passing automated tests and release audits does not prove real OPPO, Chinoppo, Magnetar, Reavon, Kodi, NAS, TV, ADB, Roku, LG, Samsung, Sony, Panasonic, Vizio, or AVR hardware behavior. In particular the v2.9.16 fixes — AVR power-on/input/restore eligibility under the `http_handoff` Pure-HTTP default, OPPO HTTP path translation before the BDMV check, the SVM3/`oppo_control`/eISCP read hardening, the honest Pure-HTTP launch-failure reporting, the distinct Samsung `KEY_HDMI1`/`KEY_HDMI2` defaults, the input-coercion crash fixes, and the AutoScript CR/LF sanitization — are software-verified only. Real tester results must be recorded separately before any hardware path is described as validated.
