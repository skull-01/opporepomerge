# Release Notes — v2.9.10 Build 11

Build 11 adds the AVR framework skeleton for future AVR support. The build is metadata and validation only: AVR control is disabled by default, no brand command execution is included, and no playback sequence calls AVR code.

This is a software build only. Real hardware validation was not performed or claimed.
