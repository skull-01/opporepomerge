# Pre-Hardware Audit Report — v2.9.10 Build 17

Build 17 keeps AVR control disabled by default, keeps AVR power-off and volume automation disabled by default, runs AVR sequencing only for external-player handoff, treats AVR/TV failures as non-fatal, and keeps hardware validation unclaimed.
