# Coverage Report — v2.9.10 Build 7

```text
TOTAL 99%
```

Coverage was collected with `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m coverage run -a -m pytest ... -p no:ddtrace` across split test-file groups to avoid plugin/runtime timeout behavior while preserving the same test set.

Key Build 7 coverage notes:

- `resources/lib/roku_ecp_control.py`: 100%
- `resources/lib/tv_control.py`: 100%
- `resources/lib/tv_presets.py`: 100%
- Project total: 99%

Post-unpack coverage also reported TOTAL 99%.
